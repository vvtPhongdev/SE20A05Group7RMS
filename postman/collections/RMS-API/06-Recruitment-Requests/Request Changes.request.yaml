$kind: http-request
name: Request Changes
method: PATCH
url: '{{baseUrl}}/api/v1/recruitment-requests/{{requestId}}/request-changes'
order: 8000
headers:
  - key: Content-Type
    value: application/json
body:
  type: json
  content: |-
    {
      "notes": "Clarify headcount"
    }
description: Request changes to recruitment request
